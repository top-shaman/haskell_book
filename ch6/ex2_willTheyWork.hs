--1. yes, 5
max (length [1, 2, 3])
    (length [8, 9, 10, 11, 12])

--2. yes, 15
compare (3 * 4) (3 * 5)

--3. no, expected type isn't the same as actual type. need to both be [Char] or both be Bool.
compare "Julie" True

--4. yes, False
(5 + 3) > (3 + 6)