module Parentheses where

--1. different
--a) 8 + 7 * 9
--b) (8 + 7) * 9

--2. same
--a) perimeter x y = x y = (x * 2) + (y * 2)
--b) perimeter x y = x * 2 + y * 2

--3. different
--a) f x = x / 2 + 9
--b) f x = x / (2 + 9)